#include "stdafx.h"
#include "player.h"
/*
player::player(const char* name, const int hp)
{
	name_ = name;
	hp_ = hp;
}

player::~player()
{
	delete name_;
	name_ = nullptr;
}

void player::damaged(const int damage)
{
	hp_ -= damage;
	cout << name_ << " received " << damage << " damages and now has " << hp_ << " health" << endl;
	if (hp_ <= 0)
	{
		cout << name_ << " say: ";
		switch (is_dead_)
		{
		case false: cout << "Ahhhhhhhhhhhhhhhhhhhhhhhhhhhhhhhhhhhhhhhh, Sh*t I'm dead." << endl;
			is_dead_ = true;
			break;
		default: cout << "I'm already dead." << endl;
			break;
		}
	}
}

const char* player::get_name() const
{
	return name_;
}
*/